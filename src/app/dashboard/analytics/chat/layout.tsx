/**
 * @fileoverview Layout for the new AI Chat page.
 */
'use client';

import type { ReactNode } from "react";

export default function AiChatLayout({
  children,
}: {
  children: ReactNode;
}) {
    return <>{children}</>;
}
